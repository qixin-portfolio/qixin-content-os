# AI Collaboration Rules

1. Read `docs/superpowers/specs/2026-07-12-qixin-content-os-design.md` and the active plan before editing.
2. Use test-driven development for domain behavior.
3. Never invent performance data, user feedback, launch state or revenue.
4. Never commit secrets, uploaded private media, local databases or generated export packages.
5. Keep user-authored text unless the user explicitly requests replacement.
6. Every completed task must include tests, lint and build verification.
