# v0.2.0 Phase 2 Release Review

日期：2026-07-12  
审查对象：`0fe5c5c feat: build reality import layer`

## 审查结论

Phase 2 release baseline 审查通过。真实项目资产导入层已提交并推送，未进入 Phase 3。

## Git 状态

- 工作区：clean
- 当前分支：`main`
- `main` 与 `origin/main`：同步
- 审查时分叉计数：`0 0`

## 验证结果

- `npm test`：8 个测试文件，18 个测试通过。
- `npm run prisma:validate`：通过，schema valid。
- `npm run lint`：通过。
- `npm run build`：通过，Next.js 生产构建成功。

## Phase 2 基线

- `ProjectSource` 已加入 Prisma schema，并关联 Project。
- SourceItem 可关联 ProjectSource，EventCard 必须保留 SourceItem 来源关联。
- Markdown 导入 API/UI 可按项目保存私有 SourceItem。
- GitHub adapter 只处理指定 repository + commitSha，不自动扫描。
- mock EventCard generator 缺少事实字段时返回 validation error。
- seed 已验证 4 个项目、4 个透明工地 SourceItem 和 1 个关联来源的 EventCard。

## 已知限制

- 本机 Prisma 7.8 的 `migrate dev`/`migrate deploy` 在 schema engine 阶段返回空错误；migration SQL 已由 `migrate diff` 生成，并用 `db execute` 验证可执行。此次 release review 不将 migration deploy 写成通过。
- EventCard generator 仍使用 mock provider，只识别结构化 Markdown 事实段落。
- GitHub adapter 依赖 GitHub API 访问权限，只支持指定 commit，不支持自动同步。
- seed 依赖本机真实资料根目录，资料缺失时会失败，不生成替代内容。
- 未实现自动发布、完整 CMS、目录扫描、webhook 或 Phase 3 内容持久化扩展。

## 后续状态

Phase 3 未开始，等待确认。
