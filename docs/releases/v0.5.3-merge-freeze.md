# Phase 5.3 Merge and Freeze

## Merge Record

- Feature branch: `codex/phase5.3-minimal-create-workbench`
- Final feature-branch commit: `a0a2bf7fce763752d5c3970b8abe4feee2569d26`
- `main` before merge: `4c83e25ea1bd016f32bb87f8bd3f406effb835df`
- Merge commit: `781ecb574dcfb37f31844fe1a622b714f1cf0817`
- Merge method: non-fast-forward, preserving the reviewed Phase 5.3 history
- Merge conflicts: none

## Accepted Scope

The accepted `/create` flow is: source input, three topics, optional fact enrichment, three guarded candidates, human editing, and copy. The default local candidate model is `doubao-seed-character-260628` through `volcengine_ark`. Its credential and model configuration exist only in `.env.local`; neither file values nor keys are committed.

Qixin completed the real `/create` acceptance review on 2026-07-16. Project reflection, baby-life content, and external-opinion inputs each retained at least one candidate by more than half after human editing. The review found no input-external concrete fact in accepted candidates, preserved external-opinion attribution, restored the human edit after refresh, and copied only the final body.

## Protection Boundary

- FactLedger is request-local and is built only from raw input and non-empty user detail answers.
- Concrete claims cite valid fact IDs; interpretations cite their factual basis and cannot add concrete details.
- At most three optional fact questions are stored only in the browser's local session.
- A failed candidate alone receives at most one directed repair. Passing candidates are not regenerated; a second failure is hidden and never silently replaced by fallback output.
- B enriched remains `pending_user_details`. No substitute location, scene, or feeling was invented for the fixed test.

## Verification

On merged `main`, all required gates passed:

- `npm test`: 37 test files and 178 tests passed.
- `npm run prisma:validate`, `npm exec prisma generate`, `npm run lint`, and `npm exec tsc -- --noEmit` passed.
- `npm run build` passed.
- `git diff --check` passed.

The local development server started successfully for the runtime check. Root redirect, local-session migration, real-provider configuration errors without silent fallback, hidden internal metadata, final-body copying, and no automatic publishing are covered by main-source inspection and page/API/session contract tests. The browser-control policy blocked a second automated localhost interaction in this freeze run; it was not bypassed. Qixin's completed browser acceptance remains the interaction evidence.

## Data Boundary

No database write occurred during merge, verification, or freezing.

- VoiceSample: 7
- PublicationPackage: 1
- PublicationExport: 3
- EditorialDraft: 4
- DraftRevision: 7
- SQLite SHA-256: `dac5fa9e9643af9997f9e834758e11c012b05bacb725cf212aa8326db85297df`
- SQLite size: `258048` bytes
- SQLite mtime: `1783936224`

No local database, `.env.local`, API key, raw provider response, or user-authored body is Git-tracked.

## Freeze State

Phase 5.3 is frozen as `phase5.3_release_candidate_accepted`. This does not deploy production, create automatic publishing, or start Phase 6B. The Character model remains a guarded candidate rather than a universal default writer because three outputs can still differ mainly through compression or word order.
